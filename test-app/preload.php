<?php

declare(strict_types=1);

/**
 * OPcache preload script.
 * Loaded once when FrankenPHP starts — classes stay in shared memory.
 */

// Preload common classes for faster request handling
// Add application classes here as the app grows
